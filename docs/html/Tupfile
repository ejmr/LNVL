# The options to give to Pandoc.
PANDOC_FLAGS += -f markdown
PANDOC_FLAGS += -t html5
PANDOC_FLAGS += --standalone
PANDOC_FLAGS += --table-of-contents
PANDOC_FLAGS += --css=Style.css
PANDOC_FLAGS += --smart
PANDOC_FLAGS += --indented-code-classes=lua,numberLines

# Convert the documentation, written in the Markdown format, to HTML.
: foreach ../*.md | *.html \
|> ^ Writing %o^ pandoc %f $(PANDOC_FLAGS) -o %o \
|> %B.html
